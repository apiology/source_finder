# SourceFinder finds source and documentation files within a project.
module SourceFinder
  # We bump the minor version when new languages or directories are
  # added; the major version when existing APIs change.
  VERSION = '3.2'.freeze
end
